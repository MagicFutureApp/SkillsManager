# 2026-09-22

## cache-manager Worker 全接口鉴权（推荐页同步 token）
- 新增专用 token `SKILLS_MANAGER_CACHE_SYNC_TOKEN`（与 admin token `SKILLS_MANAGER_CACHE_ADMIN_TOKEN` 分离）。
- Worker `src/index.ts`：加 `/api/*` 中间件，对 `/api/best-100`、`/api/best-100.csv`、`/api/status` 用 sync token 做 Bearer 鉴权；`POST /api/refresh` 跳过该中间件、仍走 admin token（独立鉴权）。`GET /` 根健康检查留作公开 liveness probe（未锁，已向用户说明）。
- 桌面 `core/best-100/constants.ts` 硬编码 `BEST_100_SYNC_TOKEN`（固定共享值，零配置，桌面自动带 `Authorization: Bearer`）；`main/ipc/best-100.ts` 的 `fetchCsvFromApi` 拉 `/api/best-100.csv` 时带该头。桌面不携带 admin token（避免越权）。
- 固定 token 值（sync）：`gmsP0i8iXCTunM0zoiHR8mPG8U6Bt8mefYkTHPeQp1U`；已同步写入 `.dev.vars` 的 `SKILLS_MANAGER_CACHE_SYNC_TOKEN`（与桌面常量同值）。admin token：`hzMZefHynofw9Gp8NMf2rM4isynBzIbrXyA1ucYZgnA`（`.dev.vars`）。
- 验证：`tsc -p apps/cache-manager/tsconfig.json` 与 `tsc -p apps/desktop/tsconfig.main.json` 均 exit 0（8GB heap）。
- 部署注意：线上 `wrangler secret put SKILLS_MANAGER_CACHE_SYNC_TOKEN` 须等于桌面常量值；`.dev.vars` 仅本地。若用户希望 sync==admin 同值，需把桌面常量也改成 admin 值（当前刻意分离）。
- 取舍：`GET /` 未锁；若用户要求"全部"含根路径，再加中间件即可。

## 桌面端 build-time 注入 env（SKILLS_MANAGER_CACHE_SYNC_TOKEN / BEST_100_DEFAULT_API_BASE）
- 关键事实：**desktop 的 main process 由 `tsc -p tsconfig.main.json` 编译（不是 Vite）**，所以 `process.env.X` 在打包后运行时读不到——打包进 bundle 的值必须在 build 时替换。
- 机制：新增 `apps/desktop/scripts/generate-build-env.mjs`，读 `SKILLS_MANAGER_CACHE_SYNC_TOKEN` 与 `BEST_100_DEFAULT_API_BASE`（优先级 process.env > 本地 `.env`(gitignore) > dev 兜底），写出 `apps/desktop/src/generated/build-env.ts`（gitignore、已加 `.gitignore` 与 tsconfig.main.json `include`）。
- `core/best-100/constants.ts` 不再写死 token/base，改为从 `BUILD_ENV` 导出 `BEST_100_SYNC_TOKEN` / `BEST_100_DEFAULT_API_BASE`。`main/ipc/best-100.ts` 的 `resolveDefaultApiBase` 简化为：dev(`VITE_DEV_SERVER_URL`)→localhost:8787，否则→`BEST_100_DEFAULT_API_BASE`（build 注入）。删掉了旧的运行时 `process.env.BEST_100_API_BASE` 覆盖（打包后无意义）。
- 接线：desktop `package.json` 的 `build:main` 与 `check` 都前置 `node scripts/generate-build-env.mjs`；`dev`/`build`/`package:*`(CI) 均经此路径注入。新增 `generate:build-env` 脚本。`.env.example` 已建作文档。
- CI：`.github/workflows/build-desktop-installers.yml` 的 `Build installer` 步骤加 `SKILLS_MANAGER_CACHE_SYNC_TOKEN` / `BEST_100_DEFAULT_API_BASE` 两个 `${{ secrets.* }}`（用户需在仓库 Settings 配这两个 secret）。
- dev 兜底 token 仍是 `gmsP...`（须与 `apps/cache-manager/.dev.vars` 的 `SKILLS_MANAGER_CACHE_SYNC_TOKEN` 一致，否则 `pnpm dev:test` 同步 401）。脚本对兜底有 `do not ship to production` 警告。
- 验证：`generate-build-env.mjs` 生成成功；CI 风格 `SKILLS_MANAGER_CACHE_SYNC_TOKEN=X BEST_100_DEFAULT_API_BASE=Y` 能正确烤进 `BUILD_ENV`；`tsc -p tsconfig.main.json` 与 `tsconfig.renderer.json` 均 exit 0（8GB heap）；源码 grep 无写死 token / 无 `process.env.BEST_100_API_BASE` 残留。

## 环境/验证提醒（沿用）
- 沙箱无 pnpm/mv；tsc/vitest 必加 `NODE_OPTIONS=--max-old-space-size=8192` + vitest `--pool=threads`，否则 V8 OOM。git 用 `git -C D:/code/skills-manager`（勿 `/d/...`）。
- workerd.exe 损坏曾导致 `wrangler dev` 报 `spawn EFTYPE`；cache-manager 那份已用根目录正常 exe 覆盖修复。

## BOM 防护（手写的 CSV parser）
- `apps/desktop/src/core/best-100/parse-csv.ts` 的 `parseCsvRecords` 入口加 `normalized = text.charCodeAt(0)===0xfeff ? text.slice(1) : text`，后续 `text.*` 全部改用 `normalized`。防止上游 `best-100.csv` 带 `\uFEFF` 时首列表头变成 `\uFEFFrank` 导致所有行 `rank` 静默为 0。其他逻辑未动。
- 维持手写 parser（不引入 papaparse 等第三方库）：schema 固定、来源可信、规模小、已按表头名映射（上游增列/换序不挂）、零依赖。
- 验证：`vitest run parse-csv.test.ts` 6/6 通过（8GB heap）。未新增 BOM 专项测试（用户要求"其他不动"）。

## 推荐页置顶 + 启动默认选中
- 需求：把 best-100「推荐」页放到左侧 nav 第一个，App 启动默认选中并显示。
- `features/shell/shell-navigation.ts`：workspace 组 items 重排，`recommended`（Star 图标）移到 `providers`(hidden) 之后、第一个可见项（原顺序 repositories/skills/targets/recommended）。
- `app/routes.tsx`：indexRoute `/` 重定向 `routePathById.skills` → `routePathById.recommended`。
- `app/route-config.ts`：`getActiveRouteId` 兜底 `?? "skills"` → `?? "recommended"`（覆盖重定向解析前首帧，避免 blank）。
- 测试同步：`app-sidebar.test.tsx` 的 visibleRouteIds 期望改为 `[recommended, repositories, skills, targets, settings]`；「icons for every visible navigation item」4→5（推荐页无 badge）。
- 验证：`vitest run app-sidebar/keep-alive` 15 passed(1 skipped)；`tsc -p tsconfig.renderer.json` exit 0（8GB heap）。keep-alive 启动首帧 `mountedRouteIds` 初始即含 recommended，无 R22 首帧空白。
