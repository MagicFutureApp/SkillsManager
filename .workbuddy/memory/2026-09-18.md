# 2026-09-18

## 审查 dev-zustand 分支未提交改动（keep-alive + zustand 共享数据桶）

改动清单（6 改 1 新）：`renderer/stores/data-store.ts`（新）、`app/keep-alive-pages.tsx`（KEEP_ALIVE_ENABLED false→true）、`features/skills/hooks/use-skills-page-state.ts`、`features/targets/hooks/use-targets-page-state.ts`、3 个测试文件。

意图：keep-alive 打开后页面不再卸载重挂载，"切 Tab 自动刷新"的隐式机制失效；于是把 `skills` + `registeredTargets` 提升到 zustand store 作为跨 tab 唯一真源，让保活页面能看到另一 tab 的增删改。

### 审查结论（待用户处理，未改代码）

**P0-1 Skills 数据全程只加载一次，无失效/刷新路径**：`listSkills`/`listTargets` 在 renderer 的唯一生产调用点是 `stores/data-store.ts:64-65`；`loadSkillsPageData` 首行 `status === "ready"` 直接返回；`reset()` 只在 3 个测试文件里被调用。Repositories 页 sync 成功后只 `loadRepositories()`（`use-repositories-page-state.ts:308-320`），不碰 store → **同步出新技能后 Skills 页永不更新，须重启应用**（HEAD 是每次切回都重新 listSkills，属回归）。

**P0-2 `skills[].targets` 快照与 `registeredTargets` 单向失同步**：Targets 页 `applyTargetsResult` 只整体替换 `registeredTargets`，不更新 `skills[].targets`。Targets 页删/转 target 后，Skills 页读 `selectedSkill.targets` 的位置全部基于过期数据：分发门禁 `use-skills-page-state.ts:272`、`getSkillDistributionState`（`skills-page-data.ts:95`）、sider checkbox（`sider:72`）、表格"目标数"列（`skills-page-main.tsx:256`）→ 按钮假就绪、数字含已删 target。

**P1-3 Targets 页初始加载 hack 脆弱**：`applyTargetsResult({registeredTargets}, registeredTargets[0]?.id)` 依赖「load effect 先于 visibleTargets effect 执行 + `.then` 微任务后到」的时序，把"fixture 顺序首个（Local project）"当契约；`registeredTargets[0]` 若不在第一页会得到 `selectedTarget = null`（注释已自认）。

**P1-4 `applyTargetsResult` 双数据源 + 名实不符**：入参 result 既用于算 `nextTargets` 做本地校验，又整体覆盖 store，而页面渲染读 store。

**P2-5** `status` 在加载 reject 后永久停 `"loading"`（catch 缺失；调用方 `void` 无 catch → unhandled rejection）。
**P2-6** `loadEpoch`/`reset` 是为测试加的生产代码，新增测试文件必须手动 reset，隐式耦合。
**P2-7** `data-store.ts` 反向依赖 `@/features/skills/components/skills-page-data`（features 也依赖 stores → 双向）；同文件混用 `@/` 与相对路径；`type TargetOption` 导入未使用（tsconfig 无 noUnusedLocals 故不报错）。

### 已做的验证（证据）

- `tsc -p tsconfig.renderer.json --noEmit` → EXIT=0。
- 三个受影响测试文件：56 passed / 1 skipped（全绿）。
- 全量 `vitest run src/renderer`：163 例，158 passed，4 failed —— 3 个 `i18n/react-i18n.test.ts`（缺 `skills.actions.addSkill`、`skills.filters.sortRecommended` 键；本次未触及 i18n 文件 → **既有失败**）+ `repositories-page.test.tsx` 首个用例全量并行时 5206ms 超默认 5000ms（**单文件跑全绿 → flaky，与本次无关**）。
- 跑测试的正确姿势（本机 bash 缺 grep/cat/head，PowerShell 工具不回显 stdout）：`cd apps/desktop && "D:/nvm4w/nodejs/node.exe" ../../node_modules/vitest/vitest.mjs run <app 内路径>`。

### 顺手发现的既有技术债

- **HEAD 上 `skills-page.test.tsx:493` 断言必然失败**：`git log -S 'v${selectedSkill.version}'` 显示 v 前缀来自 `5187b94`（"style: 统一版本号展示格式并加 v 前缀"），而 `git log -S 'v8f2c91a' -- <test file>` 为空 → 那次改 UI 没同步测试断言。本次工作区把它改成 `v8f2c91a`（顺带把 `searchSkills("Release Notes")` 统一成 `"Review Bot"`，该 helper 只断言输入框 value，无害）。
- 相关：无任何 workflow 跑 `pnpm test`，测试长期无人守护。

### 交付物

- 审查报告落盘：`2026-09-18-dev-zustand-review.md`（仓库根目录，174 行 / UTF-8 无 BOM），含改动清单、验证命令与输出、P0/P1/P2 问题分级（带文件行号证据）、修复建议、既有技术债。

### 遗留

工作区根目录留了两个我生成的临时日志 `tmp-review-test.log`（UTF-16）、`tmp-repo-test.log`，沙箱内删不掉，需用户清理。

---

## 修订（v2，用户指出 3 处事实错误，已逐条核实并全部成立）

用户复核后指出上面审查有 3 处错误。我去代码里逐一核对，**三点全部成立**，报告已改（`2026-09-18-dev-zustand-review.md` 加「修订记录」小节，标 v2）。

1. **上「P0-1 证据」中的「唯一生产调用点」表述错误**：`listTargets` 在 renderer 有**两处**生产调用点 —— `stores/data-store.ts:65` **和** `use-targets-page-state.ts:138`（`refreshTargets` 内 `rescanResult ?? (await window.skillsManager?.listTargets?.())`，手动 rescan 且 `rescanTargets` 未返回时触发）。只有 `listSkills` 是单点（`data-store.ts:64`）。该处只写 targets 不碰 skills，**P0-1 主结论不变**。

2. **上「P1-3 …会得到 `selectedTarget = null`」是错误的推论**：`applyTargetsResult` 内部已有 `nextTargets[0]?.id ?? null` 兜底（`:99`）；且 `visibleTargets` 选择 effect（`:524-537`）在 `visibleTargets.length > 0` 时会把不在可见列表的 `selectedTargetId` 强制改写为 `visibleTargets[0]?.id`，而 `visibleTargets` 每次 store 更新都会换新引用、effect 必然重跑。→ 只要 ≥1 个 target，`selectedTarget` 恒为某个可见 target，**界面不崩、详情不空白**。真实表现是「初始选中偏好被静默覆盖」：store 顺序首个（`Local project`）不在名称排序第 1 页时，会被换成 `Design scratch`。

3. **上「P0-1 因果」归属错误**：阻断刷新的根因**不是** keep-alive 切 hidden，而是 `data-store.ts:49-51` 的 `status === "ready"` 一次性加载守卫 —— store 是**模块级单例**，`status` 跨页面卸载/重挂载存活。所以「把 `KEEP_ALIVE_ENABLED` 翻回 `false`」**也恢复不了刷新**（重挂载仍命中短路）。`keep-alive-pages.tsx:29-31` 的注释只在**渲染**语义成立，**数据刷新**语义已不等价。修复方向必须是在 store 层加 `refresh()` / 变可见时刷新，而不是回退 flag。

**教训（写给以后的自己）**：写"唯一/必然/一定"这类全称判断前，必须把 grep 结果逐条看全，不要只看到自己预期的几条就下结论；写"→ 会导致 X"的因果链时，要顺着 effect 执行顺序和 state 兜底把链路走完，别停在第一跳。

**已核实的对照证据**：`git show HEAD:.../use-skills-page-state.ts` → HEAD 的 `skills` 是页面本地 `useState`（`:60`），本地 `loadSkillsPageData`（`:664`）在 `:669` 调 `listSkills`，由挂载 effect（`:70-73`）触发 → HEAD「切回即重拉」属实，但当前代码已无法靠翻 flag 回到该行为。

---

## 第二轮复审（用户已按上轮报告改完代码，出 `2026-09-18-dev-zustand-review-r2.md`）

上轮 P0-1 / P0-2 / P1-4 / P2.1 / P2.3 / P2.5 / P2.6 / §5-1 **全部落实**；P1-3 与 §4 #5（架构收敛）用户选择"不改、只在注释里说明"，均可接受。

### 新发现（写进 r2 报告）

- **R1（P1，真问题）**：`confirmDeleteRepository`（`use-repositories-page-state.ts:645-675`）删来源后只 `loadRepositories()`，**不调 `refresh()`**；而 `repositoryRepository.delete`（`db/repositories/repositoryRepository.ts:175-200`）会级联删 `skill_units`/`skill_versions`/`install_instances`/`skill_target_preferences` → 删来源后 Skills 页仍显示旧技能。P0-1 同类回归，另一条路径。
- **R2（P1）**：P0-1 的修复无测试覆盖。`repositories-page.test.tsx` 未改且没 mock `listSkills/listTargets`，`refresh()` 在其中只拉到 undefined，零断言。新增的 keep-alive 用例走的是 `setRegisteredTargets`（覆盖 P0-2）。
- **R3（P2）**：`use-targets-page-state.ts:449` 的 `preferredTargetId` 是**可证死参数** —— `applyTargetsResult` 先写桶再读桶、`adaptTargets` 是保序纯 map，故 `preferredTargetId` 分支返回值恒等于兜底 `nextTargets[0]?.id`。注释 `:443-447` 把兜底行为描述成了参数效果。（`:113`、`:492` 两处仍有用，不能整体删。）
- **R4（P2，埋雷）**：prune 口径用全量 `registeredTargets`，而 DB 侧 `skill.targets` 来自 `getEnabledTargetsBySkillId`（`skillRepository.ts:60,76`）**仅 enabled**。当前无禁用 target 的 UI 入口，故不可达；将来加"禁用 target"会在禁用场景复现 P0-2 症状。
- **R5（P2）**：`setRegisteredTargets` 现在带裁剪副作用，是"有破坏性的 setter"；传 `[]` 会清空所有 `skill.targets`（现实路径不可达，因 rescan/list 总返回全量）。
- **R6（P2）**：`data-store.ts:85` 注释写"回退到 idle"但代码是 `set({status:"error"})`；`use-skills-page-state.ts:23` 的 `type Skill` 已无用；`skills-page-data.ts:6` re-export 的 `adaptSkillRecord` 无消费者；导入风格仍不统一。
- **R7（P3）**：同步全部失败时也无条件 `refresh()`。

### 关键验证方法（本轮新增的可复用套路）

**用 `git worktree` 搭 HEAD 基线做对照**（解决"这些失败/警告是不是本次引入的"这类问题）：

```bash
git worktree add --detach D:/code/skills-manager-base <HEAD-SHA>
# 再为基线建两个 junction（PowerShell New-Item -ItemType Junction）：
#   <base>\node_modules              -> 主仓 node_modules
#   <base>\apps\desktop\node_modules -> 主仓 apps\desktop\node_modules
cd D:/code/skills-manager-base/apps/desktop && node ../../node_modules/vitest/vitest.mjs run src/renderer
```
`git worktree list` 目前只剩主仓 —— 本轮建的 `D:/code/skills-manager-base` 仍在，待用户清理（拆 junction 千万别用 `Remove-Item -Force`，会递归删主仓 node_modules）。

**本轮实测对照结果**：HEAD 基线 4 failed / 156 passed / act 警告 171；工作区 3 failed（全为既存 i18n）/ 159 passed / act 警告 54。→ `v8f2c91a` 断言由红转绿，无新增失败，警告噪音降 68%。

### 测试日志归属的坑

vitest 并行跑多文件时，各 worker 的 stderr 会交叉进同一输出流，**「An update to X」这类警告无法按文件归属**。要看归属必须 `--fileParallelism=false` 串行跑；且注意 `✓ <用例名>` 是**完成时**才打印，警告应归属到其**之后**的下一个 ✓ 行，直接取"上一个 ✓"会整体错位一档。

---

## v3：落实 §4 修复建议（用户要求"全部改一下"）

把审查报告 `2026-09-18-dev-zustand-review.md` §3 列的问题全部在代码层落实，文档 nit（§5.1 示例命令路径）也改准，并在文件加 §7 修复落实小节 + v3 修订记录。

### 代码改动
- **新增** `apps/desktop/src/renderer/stores/skill-data.ts`：中性领域模块，承载 `Skill`/`TargetOption`/`adaptSkillRecord`/`adaptTargetOption`/`pruneSkillTargets`。解 store→features 反向依赖（P2.3）。
- `stores/data-store.ts`：新增 `refresh()`（P0-1）；`setRegisteredTargets` 写桶时裁剪 `skills[].targets`（P0-2，用 `pruneSkillTargets`）；加载 IIFE 加 `catch` 回退 `status: "error"`（P2.1）；`loadSkillsPageData`→`loadSharedPageData`（P2.6）；去掉未用 `type TargetOption` 导入（P2.5）；统一 `@/` 别名（P2.4）。
- `features/skills/components/skills-page-data.ts`：从 `skill-data` 导入并 re-export `Skill`/`TargetOption`/`adapt*`，移除重复定义。
- `features/skills/hooks/use-skills-page-state.ts:75`：`loadSkillsPageData`→`loadSharedPageData`。
- `features/targets/hooks/use-targets-page-state.ts`：`loadSharedPageData`（调用点）；`applyTargetsResult` 先写 store 再读桶派生值做校验（P1-4）；挂载 effect 注释改为显式说明初始选中规则（P1-3）。
- `features/repositories/hooks/use-repositories-page-state.ts`：同步成功分支调 `useDataStore.getState().refresh()`（P0-1 刷新入口）。

### 验证
- renderer `tsc -p tsconfig.renderer.json --noEmit` → EXIT=0。
- 受影响 3 测试文件（skills/targets/keep-alive）：`56 passed / 1 skipped`，与 v2 基线一致，无新增失败。
- 用 managed node `C:/Users/andrewliang/.workbuddy/binaries/node/versions/22.22.2-3/node.exe` 跑 vitest（bash pnpm shim 坏，本机 `D:/nvm4w/nodejs/node.exe` 也可）。

### 注意
- 未 commit / push（用户未要求）。改动均在 `dev-zustand` 分支未提交工作区。
- §4 #5「store 只留数据 + 纯 setter、页面语义动作搬回 features」**未落实**：更大重构，风险高于收益，已写入报告 §7 说明。

---

## 第三轮：按 r2 的 R1–R7 改代码 + 修 r2 文档（本会话）

用户指令："改一下这个文档，然后继续改代码。" → 先修 `2026-09-18-dev-zustand-review-r2.md`，再落实 R1–R7。

### 代码（全部落在 renderer，未 commit）
- R1：`confirmDeleteRepository` + `saveRepository` 编辑分支成功路径均补 `useDataStore.getState().refresh()`（`use-repositories-page-state.ts:673` / `:594`）。
- R7：`runSyncRepositories` 的 `refresh()` 收口为「存在成功且非 skipped 项才刷新」（`:320-325`）。
- R2：`repositories-page.test.tsx` 新增断言「同步 → refresh → Skills 页更新」（`listSkills` mock A→A,B，断言 `store.skills` 含 B）。
- R4：`pruneSkillTargets` 保留集按 `enabled` 过滤（`skill-data.ts:74`）。
- R5/R6：`data-store.ts` catch 注释纠偏（`"error"` 非 idle）、`setRegisteredTargets` 隐式契约注释、`use-skills-page-state.ts` 死导入与 `skills-page-data.ts` 死 re-export 清理。

### ⚠️ R3 翻案：原"死参数"结论是错的，初版修复已引入回归并回退
- 第三轮初按 r2 原建议删除挂载点 `preferredTargetId` 实参（`applyTargetsResult({ registeredTargets })` 不带第二参）。重跑 Targets 页 4 条用例由绿转红：初始选中变成排序首个 **Design scratch**，而非 `registeredTargets[0]` 的 **Local project**。
- 根因（加调试日志核对时序）：`loadSharedPageData` 内 `set({registeredTargets})` 是 zustand 写入，会**先触发一次组件重渲染**；此刻 `applyTargetsResult` 还没执行、`selectedTargetId` 仍 null；「可见性归一化」effect（`:529-542`）把选中改成 `visibleTargets[0]`（按 sort 默认 `"name"` 排序 → `[Design scratch, Local project]`）。待 `applyTargetsResult` 运行时 `currentTargetId` 已是 Design scratch 且合法 → 走 `applyTargetsResult` 里"保留旧选中"分支（`:97-99`）而非兜底 `nextTargets[0]`（`:101`）→ **`preferredTargetId` 与兜底不等价**，它真正作用是**强制锚定初始选中**、抵消归一化 effect 的副作用。
- 原 r2 推断错在"挂载时 `currentTargetId` 必为 null"——zustand store.set 先于 `applyTargetsResult` 触发重渲染，归一化 effect 已先改了 `selectedTargetId`。
- 处置：**保留** `applyTargetsResult({ registeredTargets }, registeredTargets[0]?.id)`；注释改写陈述真实时序。r2 文档 §3 R3 已勘误并标"误判已回退"、§4 标 R3 状态为回退、新增 §7 修复落实（含 4 测试文件 `Test Files 4 passed / Tests 95 passed / 1 skipped`、tsc EXIT=0）。

### 修订 r2 文档要点
- R1 级联删除次序勘误（实际 `install_instances:188 → skill_versions:190 → skill_target_preferences:195 → skill_units:197`，原写成反序）。
- R3 整节重写：死参数 → 误判、删除会回归、保留实参。
- 新增 §7：代码改动映射 + 验证结果 + R3 回归过程记录。
- 头部加"第三轮实施"结论行。

### 临时日志（沙箱删不掉，需用户清理）
本轮：`tmp-r2-verify.log`、`tmp-r3-debug.log`、`tmp-r3-final.log`（另历史遗留 `tmp-r2-four.log` 等 5 个 + 上轮 `tmp-review-test.log`/`tmp-repo-test.log`）。

---

## 第四轮：对第三轮修复做复审 + R3 判定独立复核（本会话，出 `2026-09-18-dev-zustand-review-r3.md`）

用户要求："针对最新的修改，以及误判的原因，重新审查一下 r2 版本的问题"。**未改任何业务代码**，全部结论来自代码核对 + 变异测试 + 临时探针。

### R1–R7 核对结论（全部成立）
R1（删除 `:673` / 编辑 `:594` 补 `refresh()`）、R7（`:320-325` 与既有 `:345` skipped 判定同口径）✔；
R2 新增断言**有牙**（见变异 2）；R5/R6 注释与死代码清理到位（`type TargetOption` 仍在 `:592` 被用，保留正确）；R4 行为可接受但注释依据错（见下）；R3 回退正确。

### R3 独立复核：原判断确认是误判（我按用户报告独立复现）
三条独立证据，全部指向"`preferredTargetId` 是活的，删了会静默退化"：
- **变异 1**：`:452` 去掉第二实参 → `targets-page.test.tsx` **4 failed / 21 passed**，详情面板 heading 由 `Local project` 变 `Design scratch`。
- **探针 A/B（热 store：先 `loadSharedPageData()` 预热再挂载 Targets 页，即生产常见时序）**：带参数 = `Local project`；去掉参数 = `Design scratch`。
我原来的推理错在**默认了 `setSelectedTargetId` 更新函数里的 `currentTargetId` 会是 null**——函数式更新的 `prev` 取的是"更新被处理时"的状态，而归一化 effect（`:529-542`）的更新排得更早，已经把选中改成 `visibleTargets[0]`，所以无参数时走 `:97-99` 的"保留旧选中"而非 `:101` 兜底。
附带语义提醒：锚定值来自 DB `orderBy(asc(agentTargets.name))`（SQLite BINARY），UI 排序用 `localeCompare`，两者在大小写混排/中文名下可能不同 → "初始选中谁"目前由实现细节决定。

### 新发现（写进 r3 报告）
- **R8（P1，实测复现）**：`toggleRepositoryEnabled`（`use-repositories-page-state.ts:450-476`）成功分支**不 refresh**；而"是否启用"会改 `listSkills`（`isSkillSourceEnabled` 读 `repository_config_json.enabled`，`skillRepository.ts:57,127-134`；`update` 在 `repositoryRepository.ts:913` 写回）。UI 入口 `repository-list.tsx:159-163` 的 `<Switch aria-label="启用 {name}">`。**探针实测**：停用来源后 `listSkills` 调用次数仍为 1、桶内仍 2 个技能（重拉应为 1）。
- **R9（P1，代码推断）**：Settings「重建本地数据库」`resetLocalDatabase` 会删库重建（`main/ipc/settings.ts:117-126` → `main/app-storage.ts:64-78`），而渲染侧成功分支（`features/settings/settings-page.tsx:379-388`）不碰共享桶 → 幽灵技能/目标。
- **R10（P2）**：① `skill-data.ts:71-72` 注释引用的 `getEnabledTargetsBySkillId` 过滤的是 **`skill_target_preferences.enabled`**，与渲染侧 `agent_targets.enabled`（`targetRepository.ts:329,343` 由扫描状态决定）**不是同一字段**——真实被对齐的是 `countEnabledTargetPreferences`（`repositoryRepository.ts:806-826`，自动分发同时要求两个 enabled）。② 同时修正我 r2 的 R4 判断："无 UI 禁用入口故不可达"不准确，`path-missing`/`not-writable`/`app-missing` 扫描状态就会让 target `enabled=false`。③ **prune 是有损的**：ids 只能由 `listSkills` 补充，target 状态翻转回来时不会自动恢复，需下一次 `refresh()`。
- **R11（P3）**：`data-store.ts:31,109` 的 `setSkills` 全仓无调用方（R6 漏网）；三处 `refresh()` 未 `void`；`applyTargetsResult` 对 `undefined` 结果会把桶写空（生产不可达）；侧边栏徽标计数走独立 API 且只拉一次（`app-shell.tsx:52-58`），这轮之后会与已刷新的 Skills 页数字不一致。

### 验证数据（本轮实测）
- `tsc -p tsconfig.main.json` + `tsconfig.renderer.json --noEmit` → **0 / 0**。
- 4 个受影响文件（`--fileParallelism=false`）→ **95 passed / 1 skipped**（+1 为 R2 新用例）。
- 全量 `src/renderer` → **3 failed / 160 passed / 1 skipped**，失败全为既存 i18n 缺 key；act 警告 **54**（HEAD 基线 4 failed / 156 passed / 171）。
- 变异/探针 4 次，**全部还原**：`md5sum` 与变异前逐字节一致（`61bbf23f…` / `06d5c89f…`）。

### 可复用套路（本轮新增）
**变异测试 + md5 还原**：验证"某行代码是否真的被测试覆盖"时，先 `cp` 备份目标文件到仓外目录并记 `md5sum`，用 `Edit` 做最小变异（如删掉一个实参/一行调用），跑单文件测试观察红绿，再 `cp` 还原并 `md5sum` 比对。比"读代码推断"可靠得多（本轮正是它同时证伪了我上轮的 R3 与确认了新 R8）。
**临时探针测试**：在目标目录写 `<name>.test.tsx`（vitest include 自动收），跑完 `rm` 删除——本会话沙箱内 `rm` 已可用（与早前"删除被拦"的记录不同，删前可先 `echo test > f && rm f` 试探）。

---

## 第六轮：落实 r3 的 R8/R10/R11 代码修复（本会话，R9 跳过）

用户指令："修改吧。" → 在第五轮代码层核对成立基础上，动手改。

### 代码改动（全部 renderer，未 commit）
- **R8（P1）**：`toggleRepositoryEnabled` 成功分支（`use-repositories-page-state.ts:471`）补 `void useDataStore.getState().refresh();`，覆盖启用/停用来源→桶刷新这条漏网路径（与同步 `:324`/删除 `:676`/编辑 `:597` 一致）。
- **R10①（P2）**：`skill-data.ts:71-72` 注释改写——prune 保留集按 `agent_targets.enabled`（扫描状态），与 `getEnabledTargetsBySkillId`（skill_target_preferences.enabled）**不是同表字段**；真实对齐 `countEnabledTargetPreferences`（两 enabled 同要求）。行为不变。
- **R10②（P2）**：`refreshTargets`（`use-targets-page-state.ts:142`）重扫后补 `refreshSkills()`（store 新增，仅重拉 `listSkills` 重供货 `skill.targets`）。**初版误用整段 `refresh()`**：会重拉 `listTargets` 覆盖刚 rescan 的 `registeredTargets`，导致「rescans targets」用例回归（Codex 按钮丢）——实测踩中后改回 `refreshSkills`。
- **R11-3（P3）**：`applyTargetsResult`（`use-targets-page-state.ts:69-73`）开头 `if (!result) return;` 早退，防 mock 不全写空桶。
- **R11 死代码**：删 `data-store.ts:31,109` 的 `setSkills`（全仓无调用方）。
- **R11 void**：四处 `refresh()`（`:324/597/676/471`）均加 `void`。
- **R11 侧边栏徽标**：`getNavigationBadgeCounts` 收进 store（`badgeCounts` 状态 + `refreshBadgeCounts` 动作；`refresh`/`reset` 后自动重拉）；`app-shell.tsx` 改从 store 读 `badgeCounts`、挂载触发一次 `refreshBadgeCounts`，删本地 `useState` 与死 import。
- **R8 回归测试**：`repositories-page.test.tsx` 新增「toggles source enabled → 桶 refresh → Skills 页反映」断言，变异测试证有牙（摘 `:471` 的 refresh 即失败）。

### 验证
- renderer `tsc -p tsconfig.renderer.json --noEmit` → EXIT=0。
- 受影响 6 文件（skills/targets/keep-alive/repositories/app-shell/app-sidebar）：**6 passed files / 117 passed / 1 skipped**（+1 为 R8 新用例）。
- 变异：摘 `:471` refresh → R8 用例 failed；还原 → passed。
- 临时日志（需用户清理）：`tmp-r4-final.log`、`tmp-r4-mutate.log`。
- R9 按用户要求**跳过**，未改。

---

## 第五轮：独立代码层复核 r3 的 R8/R10/R11（本会话，read-only，未改代码）

用户指令："审核 r3 新发现的问题，除 R9 不管外，其他先从代码层面看是否正确，先出结论，先不改代码。"

逐项对照源码，**全部成立**：
- **R8（P1）**：`toggleRepositoryEnabled`（`use-repositories-page-state.ts:450-476`）成功分支 `.then((nextRepositories)=>setRepositories(nextRepositories))` 确实无 `refresh()`。链路 `repository-list.tsx:159-163` 开关 → `repositories-page-main.tsx:94` `onToggleEnabled={page.toggleRepositoryEnabled}` → hook。启用与否真改 `listSkills`：`isSkillSourceEnabled`（`skillRepository.ts:127-135`）读 `repositoryConfigJson.enabled`，`list()` 在 `:57` 用 `rows.filter(isSkillSourceEnabled)` 整批过滤。`update` 写回 enabled 见 `repositoryRepository.ts:913`。修法（成功分支补 `refresh()`）方向正确。
- **R10（P2）**：① 注释依据确错——`getEnabledTargetsBySkillId`（`skillRepository.ts:106-125`）滤 `skill_target_preferences.enabled`（`:113`），而 prune 用 `registeredTargets.filter(t=>t.enabled)` 即 `agent_targets.enabled`（来自扫描状态，`targetRepository.ts:329,343` `enabled = status==="detected"`）。两字段不同表；真对齐的是 `countEnabledTargetPreferences`（`repositoryRepository.ts:806-826`，同时要求两个 enabled）。② 有损裁剪路径成立：`refreshTargets`（`use-targets-page-state.ts:128-149`）只走 `applyTargetsResult`→`setRegisteredTargets`→`pruneSkillTargets`，**不重拉 `listSkills`**；被剥的 target id 只能靠 `refresh()`（重拉 `listSkills`，`skillRepository.ts:76` 重供货）才加回。
- **R11（P3）**：`setSkills`（`data-store.ts:31` 类型 / `:109` 实现）全仓 src 无任何消费方（仅自身两处）→ 确认死代码。三处 `refresh()`（`:324`/`:594`/`:673`）均无 `void` 前缀（对比 `use-skills-page-state.ts` 的 `void loadSharedPageData`）→ 仅风格不一致。`applyTargetsResult(undefined)`（`:69-73` `result?.registeredTargets ?? []`）→ `setRegisteredTargets([])` → `pruneSkillTargets` 清空所有 `skill.targets` → 生产因 rescan/list 至少一存在故不可达，但 mock 不全会静默清空。侧边栏徽标 `app-shell.tsx:52-58` `useEffect([])` 仅挂载拉一次 → 确认。

R9 按用户要求跳过（未审）。


---

## 第六轮：r4 复审（2026-09-18 晚，本会话，read-only，未改代码）

用户指令："已修改。请继续审查。生成 r4。" → 审查对象=当前工作区全部改动（11 文件改 + 2 新文件），产出 `2026-09-18-dev-zustand-review-r4.md`。

### 核对 r3 §7 声称的修复（逐条对照源码）
- R8 ✅ `use-repositories-page-state.ts:471` 补 `void refresh()`；**变异实测用例有牙**（摘掉 → 1 failed）。
- R10① ✅ 注释已改为真实依据（`skill-data.ts:71-75`）。
- R11 ✅ 删 `setSkills`、四处 `refresh()` 加 `void`、徽标搬进 store（`data-store.ts:34-35,112,223,227-240` + `app-shell.tsx:16,52-54`）。
- R9 ⏸ 按要求跳过（r3 §7 已记录）。
- §7 声明"6 测试文件 117 passed / 1 skipped" → 复跑一致，可复现。

### 核心发现 R12（P1）：R4 的 prune 修复**未闭环**
`skills[].targets` 有 **4 个写入点、2 种口径**：

| 写入点 | prune | 口径 |
| --- | --- | --- |
| `loadSharedPageData`（`data-store.ts:83-87`，启动/refresh 走这里） | ❌ | 宽松＝DB `getEnabledTargetsBySkillId`（只 `where skill_target_preferences.enabled`） |
| `setRegisteredTargets`（`:127-134`） | ✅ | 严＝`agent_targets.enabled` |
| `refreshSkills`（`:118-126`，rescan 后） | ❌ | 宽松，**整体覆盖 skills** |
| `applySkillTargetsResult`（`:135-154`） | ❌ | 只改单个 skill |

三条探针（全部已删）：
- **探针 C**（单独 `loadSharedPageData`，listTargets 含 `codex(enabled=false)`）：`["codex","local-proj"]` → **启动路径根本不 prune**。
- **探针 A**（store 层复刻 rescan 顺序）：`prune 后 ["local-proj"]` → `refreshSkills 后 ["codex","local-proj"]`。
- **探针 B**（渲染 `TargetsPage` 走真实 rescan，target 变 `path-missing`）：`["target-project"]` 残留；**变异 3 摘掉 `refreshSkills` → `[]`**（铁证对照）。

→ **R4 与 R10② 意图直接互斥**（一个要剥掉 disabled id，一个要把被剥的加回）；§7 说明与 `skill-data.ts:74-75` 注释互相矛盾。
→ 后果：`skills-page-main.tsx:256` 目标数 / `use-skills-page-state.ts:271` 分发门禁 / `skills-page-data.ts:49` 分发状态都按含 disabled 的口径算，而真正分发时 `countEnabledTargetPreferences` 会排除它 → **UI 与执行不一致**。
→ 治本：DB `getEnabledTargetsBySkillId` 加 `innerJoin(agentTargets)` + `eq(agentTargets.enabled,true)`，再删 `refreshSkills` 调用。

### 其余新发现
- **R13（P2）** 徽标刷新不完整：缺 `createRepository`（`use-repositories-page-state.ts:609-638`，repositories +1）与 `setRegisteredTargets`（`use-targets-page-state.ts:79`，targets ±1）。
- **R14（P2）** `refreshSkills` 无 try/catch（`:152` 处 `void` 调用 → 可 unhandled rejection），且**不参与 `loadEpoch`** → 违背该守卫「防 reset 后在途写回」的初衷；`refreshBadgeCounts`（`:235`）同样缺 catch。
- **R15（P3）** `reset()` 不清 `badgeCounts`；`const skills = storeSkills`（`use-skills-page-state.ts:59-64`）纯别名；Targets 初始加载 effect 丢了 `isMounted`（`:452-463`）。
- **R16（P3）** `applyTargetsResult` 早退（`:72-74`）连带跳过 `setHasLoadedTargets(true)` → IPC 返回 undefined 时空状态也不显示（生产不可达）。
- **R2 覆盖进展**：同步/开关两条路径已覆盖；**删除/编辑路径仍无测试**。

### 验证数据
- `tsc` renderer / main → **0 / 0**。
- 6 相关文件串行：**117 passed / 1 skipped**；4 受影响文件：96 passed / 1 skipped。
- 全量 renderer：**161 passed / 3 failed（全为既存 i18n 缺 key）/ 1 skipped（165）**，act 警告 **51**（HEAD 基线：156/4/2、act 171）。
- 变异 1（摘开关 refresh）→ R8 用例 failed ✅ 有牙；变异 2（摘 `refreshSkills`）→ **全绿** ❌ 零测试守护。
- 全部变异/探针已还原，`md5sum` 逐字节一致（`623453e5…` / `b0df9442…`），`git status` 无残留。

---

## 第七轮：独立代码层复核 r4 的 R12–R16（用户指令「看审核意见是否正确，先给结论，不改代码」，read-only）

对照当前工作区源码，r4 主张**基本全部成立**，仅一处措辞需修正：

- **R12（P1）✅ 成立**：4 写入点逐条坐实——`loadSharedPageData`（`data-store.ts:83-87`）走 `adaptSkillRecord`（仅 `targets: record.targets`，无 prune）；`setRegisteredTargets`（`:127-134`）唯一执行 `pruneSkillTargets`；`refreshSkills`（`:118-126`）整体覆盖 `skills` 不 prune；`applySkillTargetsResult`（`:135-154`）只改单 skill。DB `getEnabledTargetsBySkillId`（`skillRepository.ts:106-125`）**只** `where skillTargetPreferences.enabled`，无 `agentTargets` join → 启动路径 `skill.targets` 含 disabled target。`refreshTargets`（`use-targets-page-state.ts:134-159`）顺序确为 `applyTargetsResult`→`refreshSkills`，prune 被立刻覆盖。§3.5 读取点全部坐实：`skills-page-main.tsx:256`（`skill.targets.length`）、`use-skills-page-state.ts:271`（`targets.length===0` 门禁）、`skills-page-data.ts:49`（length>0→"ready"）；而 `countEnabledTargetPreferences`（`repositoryRepository.ts:806-826`）`innerJoin agentTargets`+双 enabled → UI/执行口径不一致成立。注释矛盾（§3.6）成立。
- **R13（P2）✅ 成立**：`createRepository` 成功分支（`use-repositories-page-state.ts:613-624`）仅 `loadRepositories`+`setRepositories`，无 `refresh`/`refreshBadgeCounts`；`setRegisteredTargets`（`data-store.ts:127`）内不刷。`refreshBadgeCounts` 调用点仅 app-shell:53 / data-store:112(refesh) / :223(reset)。
- **R14（P2）✅ 成立**：`refreshSkills`（`:118-126`）无 try/catch、无 `loadEpoch` 自增/校验；`refreshBadgeCounts`（`:235`）`void fetchBadgeCounts.then(...)` 无 `.catch`。
- **R15（P3）⚠️ 部分**：`const skills = storeSkills`（`:61`）纯别名 ✅；Targets 初始加载 effect（`:451-464`）确无 `isMounted` 守卫 ✅；`skills-page-data.ts:2,6-7` 经 feature 中转 re-export ✅。但「`reset()` 不清 `badgeCounts`」**措辞偏重**：`reset()`（`:219-224`）末行调 `get().refreshBadgeCounts()`，会按 DB 重拉徽标；真实残留风险仅在测试 mock 不全时存在，非「完全不刷」。
- **R16（P3）✅ 成立**：`if (!result) return;`（`:72-74`）在 `setHasLoadedTargets(true)`（`:76`）之前，result undefined 时跳过置位。生产不可达（`loadSharedPageData` 构造非空 `{registeredTargets}`），但耦合不必要。
- **§4.5 ✅ 成立**：`applySkillTargetsResult`（`:135-154`）直接 `set({registeredTargets: rawTargets})` 仅改匹配 skill，不 prune，与 `setRegisteredTargets` 契约相反；当前唯一调用点（`use-skills-page-state.ts:148` 加目录目标）不可达删除路径。

结论：r4 审查质量高、代码依据准确，可直接进入「修代码」（建议优先级 R12 治本 → R13 → R14 → R15/R16 收尾）。本用户未要求改代码。

### 教训
新增的 `stores/` 模块（`data-store.ts`）**没有专属单测**，只靠页面集成测试间接覆盖 —— R12 就是这么漏掉的。涉及"同一字段被多处写入、口径不同"的设计，必须写 store 级单测枚举每条写入路径。

---

## 第八轮：落实 r4 的 R12–R16 + §4.5 代码修复（本会话，22:47 补写）

用户指令："修复吧。" → 在第七轮"结论成立、可直接修"基础上动手改。

### 代码改动（renderer + db，未 commit）
- **R12（P1，治本）**：`skillRepository.ts:106-130` `getEnabledTargetsBySkillId` 加 `.innerJoin(agentTargets, eq(agentTargets.id, skillTargetPreferences.agentTargetId)).where(and(eq(skillTargetPreferences.enabled, true), eq(agentTargets.enabled, true)))`，`listSkills` 启动路径不再含 disabled target，与分发口径 `countEnabledTargetPreferences`（双 enabled join）一致，`skills[].targets` 四个写入点口径收敛为统一 enabled-only。
- **R13（P2）**：`createRepository` 成功分支（`use-repositories-page-state.ts:625`）补 `void refreshBadgeCounts()`；`setRegisteredTargets`（`data-store.ts:146`）写桶后调 `refreshBadgeCounts`（覆盖 targets ±1 与 repositories +1 漏刷）。
- **R14（P2）**：`refreshSkills`（`data-store.ts:118-126`）加 `epoch` 校验（`:121,126`）+ try/catch；`refreshBadgeCounts`（`:246`）加 `.catch`，防 unhandled rejection。
- **R15（P3）**：`reset()`（`:238-242`）`loadEpoch+=1` 且末行 `refreshBadgeCounts` 重拉徽标，不再残留旧计数；删 `const skills = storeSkills` 别名（`use-skills-page-state.ts:59-64`）；Targets 初始加载 effect（`:452-469`）补 `isMounted` 守卫，防卸载后 setState。
- **R16 / §4.5**：`applySkillTargetsResult`（`data-store.ts:163-165`）整体替换后对所有 skill 跑 `pruneSkillTargets`，与 `setRegisteredTargets` 契约对齐（只改单 skill 也 prune）。`if (!result) return` 早退保留在 `setHasLoadedTargets(true)` 之前。

### 与 r4 §7 建议的偏差（已记入 r4 §8.1）
**刻意保留 `refreshSkills`（未删）**：r4 §7 建议"删掉 refreshSkills 调用"。但 DB join 后 `listSkills` 已是 enabled-only，`refreshSkills` 重拉不再把 disabled 灌回，与 prune 不再互斥；删它会丢失"目标重新 detected 后 re-add"能力（prune 只删不补）。R4 与 R10② 的"互斥"随 DB join 自然消解。

### 验证
- `tsc -p tsconfig.renderer.json --noEmit` + `tsconfig.main.json` → **0 / 0**。
- 6 相关文件（`--fileParallelism=false`）：**117 passed / 1 skipped**。
- 全量 `src/renderer`：3 failed（全为既存 i18n 缺 key）/ 161 passed / 1 skipped；act 警告 51（HEAD 171）。
- DB 单测：`skillRepository.test.ts` 已加 R12 有牙用例（preference enabled + target disabled → list() 期望 targets: []），但沙箱 better-sqlite3 ABI 145 限制无法执行，靠 tsc + 既有用例逻辑佐证。
- r4 报告加 §8 修复落实（217–264 行）：逐条映射 R12–R16 + §4.5 + 偏差说明 + 验证数据。

### 注意
- 未 commit / push（用户未要求）。改动均在 `dev-zustand` 分支未提交工作区。
- 临时日志（沙箱删不掉，需用户清理）：`tmp-r4-final.log`、`tmp-r4-mutate.log`、`tmp-r4-db.log`、`tmp-r4-fix.log`。
- MEMORY.md 已随第八轮整合：新增「R12 核心发现」节，并在「renderer keep-alive」节补 R13/R14/R15 落实点。

---

## 第七轮：r5 复审（2026-09-18 深夜，本会话，read-only，未改代码）

用户指令："又改了一轮，你再看看。" → 审查对象=工作区（12 改 + 2 新），产出 `2026-09-18-dev-zustand-review-r5.md`（345 行）。

### 本轮改动性质
- **DB 层首次入场**：`skillRepository.ts` 的 `getEnabledTargetsBySkillId` 加 `innerJoin(agentTargets)` + `and(pref.enabled, agentTargets.enabled)` → **R12 治本**；配套新增 1 条 DB 用例。
- **`KEEP_ALIVE_ENABLED` `false` → `true`**（前几轮刻意短路的架构真正打开）。
- `applySkillTargetsResult` 补 `pruneSkillTargets`；`refreshSkills` 补 try/catch + epoch 守卫；`reset` 补清 `badgeCounts`；`createRepository`/`setRegisteredTargets` 补 `refreshBadgeCounts`。
- `use-skills-page-state.ts` -128 行、`skills-page-data.ts` -60 行（本地 state/重复类型收敛到 store）。

### 核实 r4 的 7 项
R12 ✅ 治本 / R13 ✅（但有第 3 处漏网→R17）/ R14 ✅（refreshBadgeCounts 同类未修→R20）/ R15 ✅ / R16 ⏸ 仍在（生产不可达）/ R9 ⏸ 用户已跳过 / R3 ✅ 注释已改准。

### 新增发现
- **R17（P2）** `applySkillTargetsResult`（`data-store.ts:148-172`）不调 `refreshBadgeCounts`，而 `setRegisteredTargets`（`:146`）调 → Skills 页 `addSkillDirectoryTarget`（`use-skills-page-state.ts:147-150`，`main/ipc/targets.ts:245` 会注册新 `agent_targets` 行）后 **targets 徽标不更新**。两条同类路径行为不一致。
- **R18（P1）** `loadSharedPageData` 拿到空结果也置 `status:"ready"`（`:83-87`）。**探针实测**：`skillsManager=undefined` → `{skills:0,status:"ready"}`，接口就绪**再调仍短路不重拉**；而 IPC reject → `error` 可重试（calls 1→2）。`global.d.ts:114,121` 的 `listSkills?`/`listTargets?` 都是 optional，说明"方法可能不存在"已被考虑。keep-alive 开启使"切 Tab 重挂载=隐式重试"彻底消失，`:88-93` 注释的前提已不成立。
- **R19（P2）** 保活承诺"保留滚动位置"架构上不成立：滚动容器是**共享**的 `<main overflow-y-auto>`（`app-shell.tsx:89`），被 `hidden` 的是其子 div（`keep-alive-pages.tsx:79`）＝`display:none` → 自身与后代 scrollTop 归零。jsdom 测不了，需真机。
- **R20（P3）** `refreshBadgeCounts`（`:246-259`）无 `.catch()`，调用点本轮从 1 扩到 4。
- **R21（P3）** `data-store.ts:11` import `@/features/shell/shell-navigation`，与 `skill-data.ts:7-9` "切断反向依赖"注释矛盾（纯 type import）。

### 验证数据
- `tsc` renderer/main → **0 / 0**；4 受影响文件串行 **96 passed / 1 skipped**；全量 **161 passed / 3 failed（全为 i18n 缺 key，如 `skills.actions.addSkill`）/ 1 skipped**（165），act 51。
- **DB 用例跑不了**（ABI 145 vs 137）→ 用 Python `sqlite3` 等价 SQL 验证 **5/5 PASS**（旧口径含 `t-off`、新口径排除、正常配对不误伤、孤儿偏好被剔除）。
- **prune 探针**（已删）：`loadSharedPageData` 不 prune（`[["t-on","t-off"]]`）→ `setRegisteredTargets` 按 enabled 裁（`[["t-on"]]`）→ `setRegisteredTargets([])` 全清。
- **变异 4 次**：M1 摘 `createRepository` 徽标 → 40 passed 全绿；M2 摘 `refreshSkills` epoch 守卫 → 全绿；M3 摘 `setRegisteredTargets` 徽标 + `reset` 清徽标（叠加 M2）→ 全绿；**M4 `setRegisteredTargets` 空操作 → keep-alive 用例 1 failed（有牙）**。
- 全部变异/探针已还原，`md5sum` 逐字节一致（`fa4120b5…` / `efea63de…`）；**本轮仓根日志已自行 `rm` 清掉**（发现仓根 `rm` 可用）。

### 结论
上轮问题基本落实、R12 真闭环。剩余风险集中在"**一次失败/空结果被固化为永久状态**"（R18）与"**保活页面的隐式恢复路径消失**"（R19 + R9），两者都因 keep-alive 翻 `true` 而放大。

---

## R6 复审（第六轮，报告 `2026-09-18-dev-zustand-review-r6.md`）

### 关键事实：工作区与 r5 审查对象**完全一致**，r5 §4 的 5 条建议**一条都没落地**
`data-store.ts` 关键行号对齐（`:83-87 / :146 / :148-172 / :241 / :246-259`）。所以 r6 的定位不是"审新代码"，而是**汇总 + 补漏**：r5 未修的 R17/R18/R19/R20/R21（+r4 的 R16）全部仍在，本轮又新挖 7 条（R22–R28）。

### 新发现（R22–R28）
- **R22（P2）**：`keep-alive-pages.tsx:64-66` 的 `isRendered` 只查 `mountedRouteIds`，不含"当前路由恒渲染"；而 `mountedRouteIds` 在 `useEffect`（passive，绘制后）里更新 → **切到未访问过的 Tab 必闪一帧空白**。`KEEP_ALIVE_ENABLED=false` 走 `routeId === activeRouteId` 无此问题，是翻开关引入的副作用。修：`|| routeId === activeRouteId`（1 行）。act 会 flush effects，jsdom 测不到，需真机。
- **R23（P3）**：`getTargetOptionsForSkill`（`skills-page-data.ts:23-34`）是 `skill.targets` 之外**第 5 个偏好口径读取点**——按 `skillPreferenceIds`（只看偏好行**存在**）过滤 independent target，而勾选/门禁/目标数按 `skill.targets`（**双 enabled**）。且 `adaptTargetOption` 丢弃 `skillPreferences[].enabled`，UI 无法区分"偏好存在但停用"。当前不误分发（门禁另读 `skill.targets`）。
- **R24（P3）**：`data-store.ts:173-236` 三处——(a) `toggleSkillTargetPreferenceLocally` 的 `enabled=false` 分支是死代码（唯一调用点 enabled=false 时提前 return 转弹窗）；(b) `removeSkillTargetLocally` 在"保留偏好"且 pref 不存在时**新增 `enabled: true`**，与刚落库的 `enabled=false` 写反；(c) 两处都不更新已存在 pref 的 `enabled` → store 内该字段**永远是 true**，与 DB 分裂（因字段被适配器丢弃而暂不可见）。
- **R25（P3，探针实证）**：`setRegisteredTargets` 无条件 `refreshBadgeCounts()`，而 `applyTargetsResult` 每次都调它（Targets 页 6 个入口共用）→ 每次多付 1 次徽标 IPC，即使传的是同一数组引用。实测 `badgeCalls after setRegisteredTargets = 1`（同引用）。
- **R26（P3，探针实证）**：`pruneSkillTargets` 的 `skills.map()` 永远返回新数组（单 skill 复用但外层不复用）→ 每次整体替换都让 `state.skills` 换引用 → Skills 页整表重算。实测 `skills 引用变化 = true`（无需裁剪时）。修：无裁剪时 `return skills`。
- **R27（P3）**：`use-skills-page-state.ts:427-433` 偏好写入 `void` 不 await、失败不回滚无提示；且与 `refresh()`/`refreshSkills()` 整桶重拉之间**无写屏障**（写入在途时被旧快照覆盖 → 勾选回退）。
- **R28（P2）**：`DataStore.status` **只有写入者没有读者**（全仓 4 处 selector 只读 skills/registeredTargets/badgeCounts）→ 首次加载失败时是"空界面 + 无 loading/无错误/无重试"，且 keep-alive 下"重挂载=隐式重试"已消失。与 R18 是一体两面，必须一起改。

### 验证
- `tsc` renderer/main → **0 / 0**；4 受影响文件串行 **96 passed / 1 skipped**（与 r5 一致，无新增失败）。
- 探针（已删）四项输出见报告 §5.2，支撑 R25/R26/R17 复验。
- `prettier --check apps/desktop/src/**` → **146 文件不合格，含大量未改动文件**（`git diff` 一直报 `LF will be replaced by CRLF`）→ 属**既有环境问题（疑似 CRLF）**，别当成改动引入的格式问题。
- DB 用例仍因 ABI 跑不了；R19/R22 需真机。

### 全量状态
未修 11 条：**R18（P1）**、R17/R19/R22/R28（P2）、R16/R20/R21/R23/R24/R25/R26/R27（P3）；另有 R5/R9 两条按用户判断搁置。测试守护缺口沿用 r5 变异结论（R13/R14/R15 零守护）。

### 工具链教训（本轮新增）
- **`git diff > file` 重定向会破坏中文**：输出 UTF-8 字节被按 GBK 解码 + 换行丢失，diff 里的中文注释全成乱码。**要引用中文注释就 Read 源文件，别信重定向后的 diff。**
- **bash 里 `rm` 被 `safe-bin/rm` shim 劫持且已损坏**（`dirname: command not found` / `safe_delete_main: command not found`）→ 删文件改用 `node -e "fs.unlinkSync(...)"`（可靠）。PowerShell 的 `Remove-Item` 在本会话**无输出且未生效**，也别依赖。
- 本沙箱 bash 缺 `ls / wc / head / cat / tail` 等 coreutils（`dirname` 也缺）→ 文件探测一律走 Read / Glob / Grep / node。

---

## 第十轮：落实 r6 的 R16/R17/R18/R20/R21/R23/R24/R25/R26/R27（R19/R22 跳过，本会话）

用户指令："R19 R22 不修以外，你他你确定的都修一下。" → 改代码，未 commit。

### 代码改动（renderer，未 commit）
- **R16**：`applyTargetsResult`（`use-targets-page-state.ts`）把 `setHasLoadedTargets(true)` 提到 `if(!result) return` 前。
- **R17**：`applySkillTargetsResult`（`data-store.ts:179`）末尾加 `refreshBadgeCounts()`。
- **R18+R28**：`loadSharedPageData` 接口不可用（`skillsResult`/`targetsResult` 均不存在）时置 `status:"error"`（非 ready）；`app-shell.tsx` 读 `status`、`error` 时渲染顶部错误条 + 重试按钮（调 `refresh()`）。
- **R20**：`refreshBadgeCounts` `.then` 链补 `.catch(()=>{})`。
- **R21**：`ShellNavigationBadgeCounts` 从 `@/features/shell/shell-navigation` 迁到 `@/stores/skill-data.ts`（导入 `AppRouteId` 自 `@/app/route-config`），`shell-navigation.ts` 保留 re-export；切断 `data-store→features` 反向依赖。
- **R25**：`data-store.ts` 加 `sameRegisteredTargetIds` 辅助；`setRegisteredTargets` 仅 target id 集合变化时才刷徽标。
- **R26**：`pruneSkillTargets`（`skill-data.ts`）无需裁剪时直接 `return skills`，复用入参引用。
- **R23+R24**：`TargetOption` 加 `skillPreferences:{id,enabled}[]`，`adaptTargetOption` 映射 `enabled`；`removeSkillTargetLocally` 保留偏好时把 pref `enabled` 置 false（含补 `enabled:false` 行，修正原写反 true）；`toggleSkillTargetPreferenceLocally` pref 存在时同步 `enabled`。**R23 不改 `getTargetOptionsForSkill` 过滤语义**（保留「按偏好存在」以便重新勾选，避免隐藏可启用 target 的 UX 回归；根因 enabled 丢失已由 R24 修复）。
- **R27**：`toggleSkillTargetPreference` 改 `async`，`await setSkillTargetPreference`，失败回滚（`toggleSkillTargetPreferenceLocally(...,!enabled)`）+ `showDistributionNotice("skills.actions.targetPreferenceSyncFailedStatus")`；新增该 i18n key（zh/en，`resources.ts`）。

### 验证
- `tsc` renderer/main → **0 / 0**。
- 6 相关测试文件（`--fileParallelism=false`）：**117 passed / 1 skipped / 0 failed**（skills 28、targets 25、repositories 40、keep-alive 4/1 skipped、app-shell、app-sidebar）。
- 临时日志 `tmp-r6-fix-tests*.log` 已用 node 删掉。
- 未 commit / push（用户未要求）。R19/R22 跳过（需真机）。
- r6 报告加 §8 修复落实（R16/R17/R18/R20/R21/R23/R24/R25/R26/R27 映射 + R23 处理说明 + 验证数据）。

### r7 复审（用户「改了一版」→ 报告 `2026-09-18-dev-zustand-review-r7.md`）
- **定位**：§8 那批 11 条修复的核对轮。结论——**代码层面 11 条全部落地**，无「声称改了实际没改」。
- **跨层核对 2 项**：R24「与刚落库 enabled=false 对齐」→ 直读 `main/ipc/skills.ts:82-98` upsert `set:{enabled:false}` 成立；R25 的 id 集合门禁安全（徽标 = `count(*)` 行数，与 target 内容无关，`navigation-badges.ts:24-28`）。
- **探针（已删 `stores/r7-probe.test.ts`）**：only listSkills→`ready`；no api→`error, skills=0`；error→refresh→`ready`；applySkillTargetsResult badgeCalls=1；setRegisteredTargets 同引用 1→1 / 同 id 且 enabled 翻转 1→1 / 新增 id→2；t1 翻 disabled 后 `skills[0].targets=[]`；无需裁剪时 skills 引用变化=false。
- **r7 新发现 R29–R32**：见 MEMORY.md（硬编码中文错误条 / R18 只修一半 / R23 字段零消费者 / 该批 0 测试）。
- 第十一轮独立复核（23:50）：r7 全文（§1 11 条落地 + R29–R32）逐条对照当前代码，结论全部成立，无事实性错误；跨层事实（skills.ts:81-98 upsert enabled:false、navigation-badges.ts:24-28 表级 count）亦独立核实一致。未改代码。
- **验证**：tsc renderer+main → 0/0；6 测试文件串行 → 117 passed / 1 skipped，与 r6 一致。
- **顺手升级 `fix-verification-review` skill**：§4 加「零测试文件即零守护」的静态快筛（免跑变异）；根因表加 3 行（只写不读的字段 / 修复绕过项目既有约定 / 「与 DB 对齐」类修复直读主进程写入语句）。

---

## 第十二轮：落实 r7 的 R29–R32（用户指令「继续修。」）

r7 复审（第十一轮）已确认 R29–R32 全部成立，本轮动手修，未 commit / 未 push。

### 代码改动（renderer）
- **R29（错误条 i18n）**：`i18n/resources.ts` `shell:` 组新增 `dataLoadFailed` / `retry`（zh/en 各一对）；`features/shell/app-shell.tsx` 加 `useTranslation`，错误条与重试按钮改读 key，消「全英文界面 + 中文报错」不一致。
- **R30（status 逐项判定）**：`stores/data-store.ts` 把 `!skillsResult && !targetsResult` 改为 `skillsUnavailable`/`targetsUnavailable`/`anyUnavailable` 三项；单侧缺失时**保留该项已有桶数据**且 `status="error"`，不再清 `[]`、不再静默 `ready`；与 catch 分支语义对齐，R18 老路闭环。
- **R31（删死字段）**：`stores/skill-data.ts` `TargetOption` 删 `skillPreferences` 字段 + `adaptTargetOption` 对应映射行（grep 全仓零消费者）；`data-store.ts` 内 R24 写 `enabled` 本地逻辑保留（已与主进程落库对齐，有效修复）。
- **R32（store 专属单测）**：新增 `stores/data-store.test.ts`（9 用例），覆盖 `loadSharedPageData` 三态、`applySkillTargetsResult` 刷徽标 1 次、`setRegisteredTargets` 徽标门禁（同引用/同 id 内容变/新增 id）、`pruneSkillTargets` 引用复用；把 r7 §3.2 探针改写成断言。

### 验证
- `tsc` renderer/main → **0 / 0**（修复前新测试 fixture `makeSkill` 误用非法字面量 `"detected"`，`SkillApiStatus` 仅 `ready|review|installed`，已改 `"installed"`；`makeTarget` 的 `"registered"` 在 `RegisteredTargetStatus` 合法范围内）。
- 7 受影响测试文件（`--fileParallelism=false`，含新 `data-store.test.ts`）：**Test Files 7 passed / Tests 126 passed / 1 skipped**，无新增失败（新文件 9 passed）。act 警告为既有 worker 噪音。
- r7 报告加 §8 修复落实（R29–R32 映射 + 验证数据）。
- R19/R22 仍跳过（需真机）。临时日志 `vitest-r7.log` 已用 node 删掉。

