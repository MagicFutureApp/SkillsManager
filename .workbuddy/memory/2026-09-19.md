# 2026-09-19

## dev-zustand 第八轮审核（r8）

- 产出 `2026-09-19-dev-zustand-review-r8.md`（仓根，未跟踪）。审查对象＝**整个未提交工作区**（15 改 + 3 新），不再只看某一批。新增问题编号 R33–R41。
- 结论：主线功能打通（数据源归一 / 口径归一 / 失败可见），但「enabled 翻转后谁重拉 listSkills」仍靠调用方手写 → 同类漏网从**来源侧**（r2/r3 修完）转移到**目标侧**。
- 验证：`tsc` renderer + main 均 EXIT=0；8 个测试文件 126 passed / 3 failed（3 条全为 `react-i18n.test.ts` 既有红）/ 1 skipped。
- 探针（`features/targets/r8-probe.test.tsx`，4 例全过，已删）实测：
  - **R33（P1）** 编辑目标后主进程无条件 `enabled:true`（`targetRepository.ts:232`，加 add 冲突分支 `:415`），但 `saveEditTarget` 无 `refreshSkills()`（全仓只有 `refreshTargets` 有）→ `pruneSkillTargets` 只删不补 → 「目标数列=0 / 分发按钮 disabled 提示请先添加目标 / sider 列着却未勾选」与 DB 矛盾。
  - **R34（P2）** Targets 挂载 `applyTargetsResult({registeredTargets: <桶值>}, …)` → `setRegisteredTargets([])` → 把每个 skill 的 targets 剪空，**抵消 R30「保留已有数据」**（可达性＝单侧接口不可用等部分加载，非正常流程必经）。
  - **R35（P2）** `KEEP_ALIVE_ENABLED` 翻 true 把 R22「切未访问 Tab 首帧空白」从死分支变成线上行为（`isRendered` 只看 `mountedRouteIds`，passive effect 才补；修法 `|| routeId === activeRouteId`）。
  - **R36（P3）** `refreshBadgeCounts` 是全仓唯一无 `loadEpoch` 守卫的写桶路径，`reset()` 后过期响应仍回填。
  - **R37（P3）** `applySkillTargetsResult` 缺 `if (!result) return;`（与 `applyTargetsResult` 的 R11-3 不对称）→ undefined 直接 TypeError。
- 另：**R38**（错误条作用域覆盖全部路由 + 非 sticky + 零测试）、**R39**（R32 新单测断言不覆盖 `skills[].targets`，故 R34 不可见）、**R40**（`react-i18n.test.ts` 3 条长期红；仓库仅 build workflow 不跑测试 → 「测试全绿」不可信）、**R41**（`keep-alive-pages.tsx:36-38`「保留滚动位置」与 `data-store.ts:115-117`「隐式重试已不存在」两处注释与行为不符）。
- 勘误（修正 r7 的一条推理）：sider 勾选态是 `skills-page-sider.tsx:72` 的 `selectedSkill.targets.includes(target.id)`，**不是**由偏好决定；`skillPreferenceIds` 只决定 independent 目标是否列出。故 R27 的乐观回滚在 UI 上真可见，修复有效。
- 未做：真实 Electron 验证（R35/R41-1 属真机观感）、DB 用例（better-sqlite3 ABI 债 → R33 的 DB 侧结论用直读源码佐证）。业务代码一行未动，未 commit。

## 复制动作加成功/失败提示（全局 Toast）

- 新增 `renderer/components/ui/toast.tsx`：zustand `useToastStore` + 命令式 `toast.success/error/info` API + `ToastHost`（createPortal 到 body，复刻原 SkillsDistributionToast 视觉；成功 emerald 图标、失败 destructive 且 `role="alert"`，自动消失、可关闭）。`ToastHost` 挂在 `features/shell/app-shell.tsx`（全局唯一宿主）。
- 新增 `renderer/lib/clipboard.ts`：`copyToClipboard(text): Promise<boolean>`（处理 clipboard 不可用 / writeText 失败）。
- 三处「复制到剪贴板」动作接入：targets `copySelectedTargetPath`、repositories `copyCachePath`、settings `copyPath` → `await copyToClipboard` + `toast.success/error`。三者均补 `useTranslation`（此前未用 i18n）。
- i18n `resources.ts` 的 `common` 加 `copied` / `copyFailed`（zh+en）。
- 测试：targets 加复制成功+失败 toast 断言；settings 复制用例改用 `I18nextProvider` 包裹（该测试文件原无 i18n 初始化）；repositories 加复制成功 toast 用例。
- 验证：tsc renderer 通过；app-shell(10)/targets(26)/settings(15) 全过；repositories 复制用例过，但其 `renders the repositories management surface` 排序下拉断言为 pre-existing flaky（还原到 HEAD 同样失败），与本次无关，未改动以免超出范围。
- 分发的 `copyDirectory`（main 文件复制）是内部步骤，分发结果已有汇总提示，不属本次「复制到剪贴板」范围。

## 全局 Toast 改造为 Base UI Toast（同日下午）

- 用户确认：把自写 zustand toast 换成 `@base-ui/react@1.5.0` 官方 Toast（已安装，符合 AGENTS.md「优先 base-ui 组件」约定）。
- 重写 `renderer/components/ui/toast.tsx`：删 zustand store / 手写 setTimeout 计时器 / createPortal，改用 `Toast.createToastManager()`（模块级单例）+ `Toast.Provider toastManager={mgr} timeout=5000 limit=5` + `Toast.Portal/Viewport/Root/Content/Description/Close`。命令式门面 `toast.success/error/info/clear` 保留，对外 API 不变 → 4 个 hook 调用点零改动。
- `toast.error` 用 `priority:"high"`（→ `aria-live=assertive`，正确无障碍）。`data-testid="app-toast"` 与 `data-toast-type` 保留在 `Toast.Root`。
- **测试适配（非引擎 bug）**：skills `expectDistributionToast` 原断言 `toHaveClass("fixed")` 已失效——Base UI 把 `fixed` 定位类放在 `Toast.Viewport` 而非单个 `Toast.Root`，改断言 card 类（`rounded-lg border shadow-lg`）。targets 失败用例原 `findByText("复制失败…")` 会命中「多个元素」——因为 `priority:"high"` 让 Base UI 额外渲染一个 `clip-path` 视觉隐藏的 `role="alert"` 无障碍副本，文案复刻导致 `findByText` 文档级命中两个；改为 `findByTestId("app-toast")` + `toHaveTextContent`。4 个测试文件 `useToastStore.getState().clear()` 全部改为 `toast.clear()`。
- 验证：tsc renderer EXIT=0；targets(26)/settings(15)/skills(全)/repositories 复制用例全过。唯一红灯仍是 `repositories > renders the repositories management surface` 排序下拉 pre-existing flaky（与 toast 无关，本次未动）。
- 复用的 Base UI 测试坑：`priority:"high"` 的隐藏 live region 会让 `findByText` 重复命中；断言 toast 内容一律走 `data-testid="app-toast"` 而非裸 `findByText`。
