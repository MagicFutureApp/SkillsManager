# 2026-09-19

## dev-zustand 第八轮审核（r8）

- 产出 `2026-09-19-dev-zustand-review-r8.md`（仓根，未跟踪）。审查对象＝**整个未提交工作区**（15 改 + 3 新），不再只看某一批。新增问题编号 R33–R41。
- 结论：主线功能打通（数据源归一 / 口径归一 / 失败可见），但「enabled 翻转后谁重拉 listSkills」仍靠调用方手写 → 同类漏网从**来源侧**（r2/r3 修完）转移到**目标侧**。
- 验证：`tsc` renderer + main 均 EXIT=0；8 个测试文件 126 passed / 3 failed（3 条全为 `react-i18n.test.ts` 既有红）/ 1 skipped。
- 探针（`features/targets/r8-probe.test.tsx`，4 例全过，已删）实测：
  - **R33（P1）** 编辑目标后主进程无条件 `enabled:true`（`targetRepository.ts:232`，加 add 冲突分支 `:415`），但 `saveEditTarget` 无 `refreshSkills()`（全仓只有 `refreshTargets` 有）→ `pruneSkillTargets` 只删不补 → 「目标数列=0 / 分发按钮 disabled 提示请先添加目标 / sider 列着却未勾选」与 DB 矛盾。
  - **R34（P2）** Targets 挂载 `applyTargetsResult({registeredTargets: <桶值>}, …)` → `setRegisteredTargets([])` → 把每个 skill 的 targets 剪空，**抵消 R30「保留已有数据」**（可达性＝单侧接口不可用等部分加载，非正常流程必经）。
  - **R35（P2）** `KEEP_ALIVE_ENABLED` 翻 true 把 R22「切未访问 Tab 首帧空白」从死分支变成线上行为（`isRendered` 只看 `mountedRouteIds`，passive effect 才补；修法 `|| routeId === activeRouteId`）。
  - **R36（P3）** `refreshBadgeCounts` 是全仓唯一无 `loadEpoch` 守卫的写桶路径，`reset()` 后过期响应仍回填。
  - **R37（P3）** `applySkillTargetsResult` 缺 `if (!result) return;`（与 `applyTargetsResult` 的 R11-3 不对称）→ undefined 直接 TypeError。
- 另：**R38**（错误条作用域覆盖全部路由 + 非 sticky + 零测试）、**R39**（R32 新单测断言不覆盖 `skills[].targets`，故 R34 不可见）、**R40**（`react-i18n.test.ts` 3 条长期红；仓库仅 build workflow 不跑测试 → 「测试全绿」不可信）、**R41**（`keep-alive-pages.tsx:36-38`「保留滚动位置」与 `data-store.ts:115-117`「隐式重试已不存在」两处注释与行为不符）。
- 勘误（修正 r7 的一条推理）：sider 勾选态是 `skills-page-sider.tsx:72` 的 `selectedSkill.targets.includes(target.id)`，**不是**由偏好决定；`skillPreferenceIds` 只决定 independent 目标是否列出。故 R27 的乐观回滚在 UI 上真可见，修复有效。
- 未做：真实 Electron 验证（R35/R41-1 属真机观感）、DB 用例（better-sqlite3 ABI 债 → R33 的 DB 侧结论用直读源码佐证）。业务代码一行未动，未 commit。
