# 2026-09-19

- Targets 页：「独立→全局」转换入口从独立目标的 Globe 操作按钮，迁移到「独立」scope tag 本身（点击该 tag 即打开转为全局对话框）。改动：`features/targets/components/target-list.tsx`（移除 Globe 按钮+import）、`target-badges.tsx`（`TargetScopeBadge` 新增 `name`/`onConvert`，仅 independent+onConvert 时渲染为可点击按钮，aria-label/title 复用 convertToGlobal/convertToGlobalTarget）。新增测试 `opens the convert-to-global dialog when clicking the 独立 scope badge`。验证：tsc renderer 通过；targets-page.test.tsx 27 项全绿。未提交（按规则等用户确认）。
