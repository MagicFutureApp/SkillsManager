# 2026-08-11 工作日志

## packages/utils 全量 ESM 化 + T1 迁移（包侧 + 消费方）
- `packages/utils` 从 dual ESM+CJS 改为**纯 ESM 单构建**：删 `tsconfig.cjs.json`；重写 `package.json`（`exports` 仅 `.` + `./cn`，无 CJS）；`tsconfig.esm.json` 的 `outDir` 改 `dist`（扁平）。
- 按 TODO(T1) 迁入 A 档纯函数契约（各自独立 `src/*.ts`，`.` 入口 re-export）：`clamp` / `isRecord` / `asString` / `isValidEmail` / `parseVersionSegments`+`isNewerVersion` / `normalizeSearchQuery`+`isValidSearchOwner`+`SEARCH_*` 常量。
- 测试 `cn.test.ts` + `helpers.test.ts` 共 **16 个全过**；`dist` 为扁平 ESM（无 `.cjs`、无 `require/exports` 标记）。
- 消费方接线：desktop + landing 的 `package.json` 加 `@skills-manager/utils: "workspace:*"`；`apps/desktop/src/renderer/lib/utils.ts`、`apps/landing/src/lib/utils.ts` 改为 re-export 自包（`cn` 来自 `/cn`，landing 另 re-export `asString`/`isValidEmail`），24+4 调用点零改动。tsc --noEmit：desktop main/renderer + landing 均 EXIT=0。

## 沙箱限制（重要）
- `pnpm install` 在本沙箱报 **EPERM**（node_modules 写入被拦），未完成；已**手动**建 `node_modules/@skills-manager/utils -> packages/utils` 软链验证解析。真实环境需重跑 `pnpm install` 固化软链 + lockfile（lockfile 改动本身正确）。
- `rm -rf` 被 safe-delete 拦截；清理用 PowerShell `Remove-Item` 或接受 gitignored 残留。

## 待办
- **Task #9（待用户决策）**：搜索/发布校验跨 app 语义统一。desktop 与 cache-manager 各有搜索校验实现且错误码不统一（`invalid-query` vs `invalid_query`/`invalid_limit`/`invalid_owner`），发布校验散落多处。改为共享原语 vs 全量统一需用户拍板，且会动 cache-manager 生产行为，不能擅自改。
- 前序遗留：GitHub Actions Variables 仍待用户手动建 `SKILLS_MANAGER_BASE_URL` = `https://sk.magicfuture.app`（Variables 非 Secrets）。

## 已完成：Task #9 搜索校验逻辑统一（仅逻辑，保留各自错误码）
- 用户拍板"仅统一逻辑"：desktop 与 cache-manager 都改用 `@skills-manager/utils` 的共享搜索原语（`normalizeSearchQuery`/`isValidSearchOwner`/`SEARCH_*`/`clamp`/`isRecord`），但**各自对外错误码保持不变**（desktop `invalid-query`；cache-manager `invalid_query`/`invalid_limit`/`invalid_owner`）。
- desktop `catalog-http.ts`：本地 `CATALOG_SEARCH_*` 常量 + `normalizeSearchQuery`/`isValidSearchOwner`/`clamp` 改为从包导入（re-export 为 `CATALOG_SEARCH_*` 名，下游 catalog-client.ts 与测试零改动）。
- cache-manager `search-query.ts`：本地 `search*` 常量 + `normalizeSearchQueryText` + `clamp`/`isRecord`/`ownerPattern` 改为从包导入（re-export 为 `search*`/`normalizeSearchQueryText` 名，route handler 与测试零改动）；加 `@skills-manager/utils: workspace:*` 依赖。
- **关键 TS 坑**：`export { X as Y } from "mod"` 是纯 re-export，不会在本模块内创建局部绑定 `Y`，模块内部引用 `Y` 会报 "Cannot find name"。正确写法：先 `import { X } from "mod"` 再 `export const Y = X`。已用此修法通过。
- 验证：desktop main/renderer/cache-manager 三处 tsc 全 EXIT=0；`catalog-http.test.ts` 37 个 + `search-query.test.ts` 36 个单测全过（错误码断言保持原样），确认行为零回归。
