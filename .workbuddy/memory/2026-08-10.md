# 2026-08-10 工作日志

## desktop 主进程 + preload 全量 ESM 化收尾

- 用户确认「全部改为 ESM」，含 preload。把 `preload.ts` 重命名为 `preload.mts`（→ 编译产物 `preload.mjs`）。
  - 关键坑：Electron 的 preload **忽略** `package.json` 的 `"type":"module"`，只认扩展名。之前编译出的 `preload.js` 即便内容用 `import` 也会被当 CJS 解析，启动崩溃（`SyntaxError`）。守护测试只校验 config 指向 + `sandbox:false`，没真正加载 preload，曾一度漏掉。
  - 修正：`tsconfig.main.json` include 加 `src/main/**/*.mts`；`window-menu.ts` 指向 `preload.mjs` + `sandbox:false`；守护测试断言 `preload.mjs` 与 `sandbox===false`；AGENTS.md 规则改为「preload 必须 `.mts`→`.mjs`」。
- `electron-builder.yml` 加 `asarUnpack: ["**/*.mjs"]`，避免 ESM `.mjs` 在 asar 内加载的已知坑（待下次实际打包验证）。
- 验证：`tsc -p tsconfig.main.json --noEmit` EXIT=0；探针编译确认 `dist/.../preload.mjs` 头部为 `import { contextBridge, ipcRenderer } from "electron"`（真正 ESM）；`window-menu.test.ts` 7/7 通过。
- 顺手清理了前序遗留的陈旧探针产物 `dist/verify-probe` 与 `apps/desktop/dist`。
- 所有改动为未提交工作区改动（按规则不主动 commit）。前序遗留项：packages/utils T1 迁移、GitHub Actions Variables 人工阻塞项，仍待用户处理。
