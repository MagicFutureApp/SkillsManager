# 2026-09-16

## 修复：Skills 页面批量分发按钮不可点击

- 问题：Skills 分发画面勾选「部分有目标 + 部分无目标」的技能时，页面级「分发选中的技能」按钮仍 disabled。
- 根因：`apps/desktop/src/renderer/features/skills/components/skills-page-data.ts` 的 `getSelectedSkillsDistributionState` 用 `selectedSkills.every(...)` 要求**所有**勾选技能都有 target，导致只要有任一技能无 target 就整体 disabled。
- 修复：改为 `selectedSkills.some(...)`（任一勾选技能有 target 即可启用）。后端 `distributionRepository.buildPreviewItems` 本就对无 target 的技能产出 0 个 preview item，故批量分发只分发有目标的技能，无需改后端。
- 验证：`skills-page.test.tsx` 全量 28 例中 27 通过；`implements distribution button states before opening confirmation`(L1464-1470) 断言「Review Bot(无target)+Design Helper(有target) → 按钮 enabled」，此前因 `.every` 回归失败，现已通过。
- 残留：L489 `renders indexed skills returned by the Electron API` 仍失败（技能详情面板版本文本 `8f2c91a` 未渲染），与本次改动无关、属预先存在的独立问题，未处理。

## 抢救：沙箱 git 仓库损坏（refs/ 丢失 + dev 历史对象缺失）

- 现象：`git status`/`git rev-parse` 报 `fatal: not a git repository`。根因是 `.git/refs/` 整个目录被删（含 `refs/heads`、`refs/tags`、`refs/remotes`），git 判定仓库无效（有效仓库必须含 `refs/`）。
- 更糟：dev 分支最近一段提交对象在本地对象库也缺失——`b42731d1`(FETCH_HEAD 记录的 origin/dev)、`e54b076b`(切换Tab keep-alive)、`3e721807`(docs memory) 等 6 个提交在 3 个 pack + loose 里都搜不到；`fsck` 还报 index cache-tree 指向丢失 tree、若干 blob missing。main/cache-manager/cache-manager-v2 的尖端对象完好。
- 抢救步骤（只建文件、不动工作树、不删东西）：
  1. `mkdir -p .git/refs/heads .git/refs/tags .git/refs/remotes/origin` → git 立即重新识别仓库。
  2. 从 `logs/refs/heads/<b>` reflog 正序遍历，对每个 `to` SHA 用 `verify-pack -v`+loose 检查存在性，取**最新且对象还存在的**提交作为该分支恢复指针。
  3. 结果：dev→`a040d81b`（再往上 6 提交全丢）、main→`e76bfb89`、cache-manager→`d66ea640`、cache-manager-v2→`c015dfeac`；remote-tracking 按 FETCH_HEAD/存在性补回。
- 关键结论：**丢失的提交内容其实都在工作树/索引里**（`git status` 列出 22 个改动路径，即那 6 个提交的内容 + 未提交改动），并未真正消失。dev 上仅 `e54b076b`/`3e721807` 两个本地提交从未 push，其余至多 `b42731d1` 已在 GitHub。
- 推荐用户侧彻底恢复：`git fetch origin` 后 `git reset --soft origin/dev`（**务必 --soft，勿 --hard**，否则会丢弃工作树里那两个未推送提交的改动）；fetch 从 GitHub 补齐缺失对象。沙箱内未自动跑 fetch（涉及远端 + 网络）。
- 教训：这又是沙箱 git/junction 损坏的复发；先 `git rev-parse` 判「not a git repository」多半是 `refs/` 没了，而非 `.git` 没了。

## 修复：reset --soft 后「暂存文件看不到」——索引 blob 丢失（带 CRLF 坑）

- 现象：用户执行 `git reset --soft origin/dev` 后，觉得暂存文件"没了"，`git diff --cached` 报 `fatal: unable to read <sha>`。
- 澄清：**`reset --soft` 永不碰 index/工作树**，暂存内容没丢。真正原因是 `.git/objects` 里那 7 个已暂存文件的**内容 blob 整段丢失**（fsck 报 7 个 missing blob），git 读不出内容做 diff，造成"文件没了"的错觉。文件磁盘内容完好（`git status` 里是 `M `/`A ` = 工作树==暂存区）。
- 定位：把 `git ls-files -s` 与 `git ls-tree -r HEAD` 导出文件，`git fsck --full` 列 missing blob，用 Python（沙箱里 grep 不可用）比对，确认 7 个 missing blob 正好对应当前 7 个暂存文件。
- **CRLF 坑（关键）**：仓库开了 CRLF 自动转换。直接 `git hash-object -w <file>` 重算磁盘文件时，磁盘是 CRLF、算出的 SHA 与索引里记录的「clean 过滤器转 LF 后的原始 blob」对不上，只有磁盘恰为 LF 的文件（如 `.workbuddy/memory/2026-09-15.md`）能重建成功，其余仍报缺。
- 正确修复：`git add --renormalize -- <7个文件>`——走 clean 过滤器把磁盘内容重算成 LF blob 写回索引，正好重建原始 SHA、且不碰其他文件。执行后 `git diff --cached --stat` 正常（7 文件，215 插/51 删），`fsck --full` 缺失计数归 0，那两个未暂存文件和未跟踪文件原样未动。
- 结论：本类"索引 entries 在、blob 在 objects 里丢了"的问题，首选 `git add --renormalize`（而非 `hash-object -w`）；`reset --soft` 是替罪羊，元凶是沙箱 `.git/objects` 损坏。

## 暂时短路 keep-alive 能力（架构全留）

- 需求：把「切换 Tab 保留页面状态」暂时回退，但架构都留着，只短路最后复用的逻辑。
- 做法：`renderer/app/keep-alive-pages.tsx` 新增模块常量 `KEEP_ALIVE_ENABLED = false`。`false` 时渲染集合 = `[activeRouteId]`（切 Tab 照常卸载，等价改造前 `<Outlet/>`）；`true` 时 = 累积的已挂载集合（只切 `hidden`）。累积 `mountedRouteIds` 的 effect 同样被该常量短路，所以一个常量控制全部行为。
- 刻意保留不动的：`KeepAlivePages` 作为 root 渲染出口、`routeIds` 顺序、`data-testid="keep-alive-page-<id>"` 约定、路由侧改动（子路由只留 path + `/` 走 `beforeLoad` redirect）。
- 测试改造：`keep-alive-pages.test.tsx` 按该常量**分流成两个互斥 describe**（`describe.runIf(KEEP_ALIVE_ENABLED)` / `runIf(!KEEP_ALIVE_ENABLED)`），新增短路口用例断言「切走即卸载 + `query` 归零 + `listSkills` 调 2 次」。翻开关即自动换覆盖目标，不会留一堆红测试。
- 验证：`pnpm run check` exit 0；`src/renderer/app` + `app-shell.test.tsx` 定向 11 passed / 2 skipped；**把开关临时翻 `true` 复跑 → 2 passed / 1 skipped**（证明双向可切、架构未坏），再翻回 `false` 复跑一致；全量 `src/renderer` 149 passed / 4 failed / 2 skipped，与既有基线同（4 个失败=react-i18n ×3 + skills-page 版本文案 `8f2c91a`）。
- 提交：已于 `e82ca50 refactor(renderer): 暂时短路 keep-alive 页面复用` 提交（用户 2026-09-16 确认）。提交时 HEAD = `ecfbd47 fix: 修复批量分发按钮门槛并新增页面 keep-alive 能力`。

## 修复：同步窗口完成后面板不关、完成项图标应为对号

- 问题：来源同步完成后，进度窗口未按预期关闭；已完成的技能/来源项仍显示转圈图标（而非对号），且完成态没有可点的「确定」按钮。
- 根因：① `use-repositories-page-state.ts` 有一段 effect，在 `syncProgressDialog.status === "completed"` 后 3000ms 自动 `setSyncProgressDialog(null)` 强行关窗（用户不想要自动关）；② `repositories-page-sync-progress-dialog.tsx` 每个 item 与标题图标统一用 `LoaderCircle`，完成态没有换成对号。
- 修复：
  - 删掉自动关闭的 effect（不再 3s 后自动关）。
  - 标题与逐项图标按状态分流：`completed` → `Check`（`text-success`）、`failed` → `X`（`text-destructive`）、`syncing` → `LoaderCircle` 旋转。
  - 完成态新增「确定」按钮（复用 `closeSyncProgressDialog`，文案取新增 i18n `syncProgress.done`="确定"/"OK"）；失败态仍保留「关闭」按钮。未完成（syncing）不显示按钮。
  - 新增 `success` 主题色：`styles.css` 的 `:root`/`@theme inline`/`.dark` 加 `--success`/`--color-success`（oklch 绿），与既有 `text-destructive` 红对称。
- 验证：renderer 类型检查 `tsc -p tsconfig.renderer.json --noEmit` exit 0；`repositories-page.test.tsx` 36/36 通过（含「shows per-skill sync progress and closes the progress dialog after completion」已改为点「确定」按钮关闭、「keeps a completed skill loader spinning for at least one second」仍过）。沙箱 rm 被 safe-delete 包装拦，临时日志 `skills-test.log`/`skills-tsc.log` 留在仓库根，需用户真实环境删除。
- 未提交（用户未要求）。

## 修复：同步进度弹窗完成体验（对号 + 确定按钮）

- 问题：同步完成后弹窗 3s 自动关闭、完成项无对号、无确定按钮。
- 改动：`repositories-page-sync-progress-dialog.tsx` 完成/失败项图标分流（Check 绿 `text-success` / X 红 / LoaderCircle 转）；完成态新增实心「确定」按钮（Button `default` variant，黑底白字，用户指定样式）；失败态保留 outline「关闭」。删除 `use-repositories-page-state.ts` 的 3s 自动关闭 effect。`styles.css` 新增 `--success`/`--color-success`（明暗两套）。i18n 新增 `syncProgress.done`（确定/OK）。
- 追加：用户要求确定按钮用截图中的实心黑按钮样式 → 直接用 `variant="default"`（`--primary`=oklch(0.205 0 0) 近黑）。
- 验证：`repositories-page.test.tsx` 36/36 通过、renderer `tsc --noEmit` exit 0（bash + 根 vitest + `--root` + `D:/` 路径方式跑通）。
- 残留：沙箱删不掉的临时日志 `skills-test.log`/`skills-tsc.log` 在仓库根，待用户真实环境删除。未 commit（用户未要求）。

## 变更：同步前二次确认弹窗（pending → syncing → done）

- 需求：点同步先弹确认窗（内容=原同步弹窗），按钮 [取消] [开始同步(实心)]；点开始同步→按钮禁用直到完成，完成后变 [确认] 关闭。
- 实现：dialog state 新增 `pending` 状态。`syncRepositoriesByIds` 非本地来源改为 `openSyncConfirmDialog`（只开 pending，不启动同步）；新增 `startSyncProgress(ids)` → `runSyncRepositories`（原名 executeSyncRepositories，行为不变：置 syncing 并跑同步）。本地来源走原本地确认窗 → `runSyncRepositories`（无确认步骤，保持一致）。
- 按钮逻辑：`pending`→[取消(outline)][开始同步(default)]；`syncing`→[开始同步(default,disabled)]；`completed/failed`→[确认(default) 关闭]。头部图标 pending 静态 LoaderCircle。
- i18n：syncProgress 新增 pendingDescription/start/cancel；done 由「确定」改为「确认」(Confirm)。localSyncDialog.confirm 仍是「确定」(不动)。
- 验证：repositories-page.test.tsx 37/37 通过（新增「确认前先询问、取消不启动」用例；原依赖"点同步即同步"的 4 条非本地用例改为点开始同步；batch skip-already-syncing 用例改写）；renderer tsc --noEmit exit 0。
- 残留：skills-test.log/skills-tsc.log 沙箱删不掉，待用户真实环境删。未提交（用户未要求）。
